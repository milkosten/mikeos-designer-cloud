# HTTP API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Async PostgreSQL
asyncpg>=0.29.0

# GPU (Ollama) + legacy X-API-KEY resolver
httpx>=0.26.0

# Programmatic sanitizer — the real self-contained guardrail
beautifulsoup4>=4.12.0
lxml>=5.0.0

# account.osmike.com RS256 JWTs via JWKS (pyjwt[crypto] pulls in cryptography)
pyjwt[crypto]>=2.8.0
cryptography>=42.0.0

# thumbnail downscaling (optional at runtime — chrome.py falls back to raw bytes if absent)
Pillow>=10.0.0
